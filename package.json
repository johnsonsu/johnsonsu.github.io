{
  "name": "gh-pages",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"johnsonsu.com\" >> out/CNAME && git add out/ && git commit -m \"Deploy Next.js to master\" && git subtree push --prefix out origin master"
  },
  "dependencies": {
    "marked": "^0.8.2",
    "next": "^9.5.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0",
    "eslint": "^6.8.0",
    "eslint-plugin-react": "^7.20.5",
    "prettier": "^1.19.1"
  }
}
